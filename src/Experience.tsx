import { DEFAULT_FONT } from "./constants";
import NotFound from "./NotFound";

export default function Experience() {
  return (
    <NotFound message='I have not yet created this experience page. Coming soon!'/>
  );
}